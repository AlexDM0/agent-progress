# `cli/` — the command surface

Everything a person or an agent types, and nothing else. The rules that matter here are the two
boundaries this folder owns; the rest of the conventions are in the root `CLAUDE.md`.

## Exit codes mean one thing each

The tool answers a calling script with a number, and the number is decided in exactly one place —
`cli/Main.ts`, from an `OperationRefusal`'s status.

| code | meaning | where it comes from |
|---|---|---|
| **0** | done, or there was nothing to do | a handler that returned |
| **1** | a refusal the caller can act on | `OperationRefusal` with status `refused`, and an unknown command word |
| **2** | a state the tool will not repair on its own | `OperationRefusal` with status `unrepaired`, and any other thrown error |

`--help` and `-h` are honoured after a command word as well as in place of one, and print the same
one screen: there is no per-command help, because a second help surface is a second thing to keep in
step with the table.

A command never chooses its own code and never calls `process.exit`: it writes through its
`CommandContext` and throws `OperationRefusal` when it cannot continue. `agent-progress.ts` at the
repository root is the only file in the repository that exits, and it does it with the number
`runCommandLine` returned. The help goes to standard output when it was asked for and to standard
error when it follows an "Unknown command" — a mistyped command that exits 0, or prints a help
screen into a pipe somebody is parsing, is the failure this arrangement exists to prevent.

## Every mutating command renders under the lock

`init`, `task`, `ticket`, `log`, `concurrency`, `dispatcher`, `range`, `release` and `clear` all follow one sequence, and the order of its
last three steps is the whole of the concurrency design:

```
requireWorkspace → withLock → readProgressFile → mutate → writeProgressFile → write the tickets → rerenderDashboard
```

It is written once, in `cli/CommandSupport.ts`'s `openTrackerForWriting`, and no command repeats it.
**Rendering happens inside the lock**, from the file on disk rather than from the value just written:
two commands that each wrote and then rendered outside the lock can interleave so that the *older*
render lands last, leaving a page that disagrees with the file it came from until somebody runs
another command. **Ticket files are written after the progress file**, so the progress file is never
behind the tickets — the direction `ensureTaskForTicket` repairs — which is why a command queues them
through `writeTicketAfterwards` instead of writing them itself.

`render` and `open` render without mutating and take the lock for that alone. `status` takes no lock
and renders nothing: every file it reads is written atomically, so a read either sees the old file or
the new one, and the command an agent runs most often should not wait behind somebody else's write.

A store write that succeeded while the render could not finish — a page bundle that would not build,
a ticket file an agent left malformed — is reported on standard error at **exit 0**. The tracker was
changed; saying otherwise would make an orchestrator re-run a command that had already worked.

## A command takes its context, it does not reach for the process

`currentDirectory`, `now()`, both output streams, whether standard input is a terminal, the read of
everything piped in and the confirmation prompt all arrive in the `CommandContext`. That is what lets every command spec run in
the test process against a scratch directory and a frozen clock. The environment is not in the
context: `lib/platform/Environment.ts` is the one module that reads it.

## Per-file

| file | what it is |
|---|---|
| `cli/Main.ts` | `runCommandLine(commandLineArguments, context)`: rewrites a missing command word and a `--help`/`-h` anywhere before a bare `--` to `help`, dispatches through the table, maps a refusal to an exit code |
| `cli/Main.spec.ts` | the four routes and the number each produces, against a captured context. The throwing command is a `mock.module` stub of `render`, mocked back to the real exports after its cases because Bun keeps a module mock for the whole run and `mock.restore()` does not lift it; a last case renders for real to prove it |
| `cli/CommandSupport.ts` | the sequence every mutating command follows, `resolveAtOption`, `printEntity`, `renderDashboard`, `renderDashboardOrRefuse` (for `render` and `open`: an unreadable tracker thrown as `unrepaired` instead of reported), and `progressOperations` — the progress store in the shape `lib/tickets/TicketTransitions.ts` asks for. Also `TICKET_STATUSES_NO_AGENT_WORKS_AGAIN` (delivered, abandoned: where `ticket agent`, `hold` and `unhold` refuse and `heldTicketIds` stops listing), `concurrencyDocumentOf` (the one place readiness, the held ids and the dispatcher state are read, for `status --json` and the Next line), `readyTicketsOf` (the ready ids resolved to `{id, priority, model, effort}`, plus `held: true` on a held one, built from `readyTicketIds` so the two never disagree), `nextLineFor` (which adds the ready ids that are low priority, for the Next line's triage advice), `openTrackerForWritingThenReadNextLine` — the same sequence, reading the Next line and the dispatcher state from the files just written, inside the lock — and `printEntityThenNextLine`, which drops the line under `--json`. The helpers more than one command shares live here too: `requireProgressFile` (the lock-free read of `status`, `concurrency` and `dispatcher`, an unreadable file thrown as `unrepaired`), `tokenCountFrom` (`--tokens`), `padColumn` (the human tables) and `ticketDocumentOf` (a ticket's frontmatter with its `priority` spelled out and its `filePath`, for every `--json` ticket) |
| `cli/NextLine.spec.ts` | the Next line ending the human output of `status`, `ticket add\|claim\|review\|deliver\|abandon\|depends`, `task finish\|deliver` and `task add --start`, table-driven with the dispatcher running so no advice is appended, each describing the board after the move, the running dispatcher notice after `ticket add`'s set aside; `ticket hold` and `unhold` taking a ticket out of `ready:` into `held:` and back, and a finished board whose only ready ticket is held advising no launch; and none of their `--json` documents carrying it |
| `cli/CommandContext.ts` | the interface every handler is given, and `createProcessContext()` for the real process |
| `cli/CommandTable.ts` | name → lazy loader for every command; `COMMAND_NAMES`; `commandLoaderFor` guarded by `Object.hasOwn` |
| `cli/CommandTable.spec.ts` | every loader resolves to a function, and an inherited property of the literal is refused rather than run |
| `cli/HelpText.ts` | `helpText()`: the whole command reference, one screen, interpolating nothing |
| `cli/HelpText.spec.ts` | the help and the table agree in both directions; the entry layout that makes that checkable |
| `cli/BinarySmoke.spec.ts` | the only suite that spawns the real binary: help, an unknown word, an inherited property, a bare-repository `init` refusal, and one whole session — init through `ticket deliver`, with a pause, a `--tokens 12k` and a matrix refusal — ending on the `status --json --full` document — and `ticket unhold`'s resume hint, printed for a paused build only and never under `--json` |
| `cli/arguments/ArgumentParser.ts` | the closure factory: flags, options in both spellings, positionals, the bare `--`, and the refusals |
| `cli/arguments/ArgumentParser.spec.ts` | the shapes that fail quietly when the parser is wrong |
| `cli/arguments/OptionsWithValues.ts` | which option names take the next argument, across every command |
| `cli/TrackerRefresh.ts` | `refreshTrackedRepository`: the managed CLAUDE.md block, the bundled `agent-brief.md` and the `SubagentStop` hook, which `init` and `update` both write through. Each line reports `unchanged` or `updated` from the file's bytes before and after; the hook goes to `.claude/settings.local.json` unless the shared `.claude/settings.json` already holds one, and `--no-hooks` skips it. The dispatcher workflow is written byte for byte from `templates/workflows/AgentProgressDispatch.js`, read at run time, to `.claude/workflows/agent-progress-dispatch.js` through the atomic writer, undoing a hand edit; `--no-workflow` skips it. The worker agent definition is `templates/AgentProgressWorker.md` with its `{{model}}` and `{{effort}}` filled from `lib/constants/AgentSettings.ts`, written to `.claude/agents/agent-progress-worker.md` the same way; `--no-agent-definition` skips it |
| `cli/init/InitCommand.ts` | `init`: discover the root, ignore the tracker, create it, render, then the shared refresh. A re-run at the same root is that refresh alone and names `update`; one below an existing tracker, one inside a bare repository, and a `--root` that is not an existing directory are all refused |
| `cli/init/InitCommand.spec.ts` | the tree and all its side effects, `--no-claude-md`, `--no-hooks` and the still-accepted `--hooks`, the workflow installed byte-identical and `--no-workflow`, the agent definition's default pair and `--no-agent-definition`, `--root`, the re-run and the line pointing at `update`, the two refusals, and the worktree case that the one-tracker-per-repository promise rests on |
| `cli/update/UpdateCommand.ts` | `update`: the shared refresh on its own, at the tracker found the way every command finds it. No tracker is a refusal at exit 1 naming `init`; it writes nothing the tracker holds and takes no `--project` or `--root` |
| `cli/update/UpdateCommand.spec.ts` | each item's `unchanged`/`updated` verdict against a file that really differs, the hook in the local settings file by default and refreshed in place in the shared one, the workflow and the agent definition each unchanged on a second run and restored after a hand edit, `--no-claude-md`, `--no-hooks`, `--no-workflow` and `--no-agent-definition`, the progress file and tickets left byte-identical, the worktree, and the refusals |
| `cli/status/StatusCommand.ts` | `status`: the working view — counts, the rows and tickets that are not delivered or abandoned, the recent log ordered by its stamps, and under `--json` an `omitted` count of what was left out. `--full` is the *whole progress file plus every ticket's frontmatter*. Both `--json` documents carry the derived `concurrency` block: `limit`, `agentsInFlight`, `freeSlots`, `readyTicketIds` (in dispatch order, high first), `dispatcherState` and `heldTicketIds` (every held ticket not delivered or abandoned, whatever its status), plus `dispatcherRunId` while one is stored, and beside it `readyTickets`, the same tickets in the same order as `{id, priority, model, effort}` with the defaults resolved; every ticket in them spells out its `priority`; the human output ends with the Next line built from it |
| `cli/status/StatusCommand.spec.ts` | both `--json` documents' shapes, which are the contract with the orchestrating agent, the concurrency block and the run id it carries only while one is stored, `readyTickets` resolved and in the order of `readyTicketIds`, plus the token column and the log's order |
| `cli/task/TaskCommand.ts` | `task add\|start\|pause\|finish\|review\|rereview\|deliver\|update\|remove`: the rows that are not a ticket's. `update` is the one that does not move the row's clock — and, for the same reason, the one that files no phase in the row's `history`: it corrects a row rather than moving it, and a correction is not something that happened. `rereview` counts a row's second and later review passes, and a row a ticket owns is refused unless `--force`. `add --review-of <id>` stores the ticket a review row reviews in its `reviewOf` field — a relation apart from `--ticket`, which leaves the ticket file alone — and refuses a ticket that does not exist at exit 1 before anything is written |
| `cli/task/TaskCommand.spec.ts` | the lifecycle, that a repeated stamp does not move, the review rounds `rereview` counts, pause and resume, `--tokens`, `--at`, `--json`, the ticket-owned refusal, the link rules on both sides, and `--review-of` stored and shown by `status --json --full` or refused with the progress file byte-identical |
| `cli/log/LogCommand.ts` | `log`: one line, joined from every positional so an unquoted sentence is not truncated to its first word |
| `cli/log/LogCommand.spec.ts` | the joining, the backfill, and the refusal of an empty line |
| `cli/hook/HookCommand.ts` | `hook subagent-stop`: the `SubagentStop` hook, reading the hook JSON from standard input and appending one line saying what the finished subagent cost, and adding its `input` total to each row the brief's `agent-progress row: <ids>` line names, or to the row each ticket on an `agent-progress ticket: <ids>` line holds when the hook runs, or to the newest row reviewing the ticket an `agent-progress review: <id>` line names — by `reviewOf` or by name, through `reviewedTicketNumberOf` of `lib/render/page/PageMarkup.ts`, whatever its status — each looked up in the same lock hold. A brief with several lines is read by one alone: `row:` over `ticket:` over `review:`. The tracker is resolved from the hook input's own `cwd`. **The only command that exits 0 on every failure** — no input, bad JSON, no transcript, no tracker, no calls — because the agent has already finished when this runs, so a non-zero exit prevents nothing and buys only an error the orchestrator has to read and a delay before it is told |
| `cli/hook/HookCommand.spec.ts` | the line it writes, the row tokens it adds — twice over on a second run, split over a bundle, never from a marker after the brief, a missing row named and skipped, a workflow agent's path; the ticket form resolved to a row created after the brief, split over a bundle, a ticket with no row or none at all named and skipped, and the row line winning over the ticket line; a workflow agent's computed task read as the brief after the harness's relay, and nothing read after a relay otherwise; the review form resolved to a row filed after the brief and delivered, the later of two review rows, no review row named and skipped, and both other lines winning over it — each of the seven failures — the held lock among them — leaving the tracker untouched at exit 0, the `cwd` the tracker is found from, and the one refusal: a misspelled event |
| `cli/usage/UsageCommand.ts` | `usage`: what the subagents of this repository cost, profiled from the transcripts under `~/.claude/projects/`, a workflow's agents included. One row per agent oldest first, then the cohort summary; the `over 200k`, `bash edits` and `checks` columns are the three a breached brief shows up in. `--since` splits the cohort and summarises both sides, `--transcripts` names a folder instead of deriving one. Read-only — no lock, no write, no render — and finding nothing is one sentence at exit 0 |
| `cli/usage/UsageCommand.spec.ts` | the rows and the summary against a scratch folder of constructed transcripts, a workflow run beside a plain agent counting three agents and no journal, the `--since` split, the `--json` shape, the oversized share, bash edit and check columns against a transcript that breaches the brief, an empty folder, and the refusal of an unreadable `--since` |
| `cli/rework/ReworkCommand.ts` | `rework`: the lines of code a review reworked — `--since` over `<commit>..HEAD`, `--rebased-from` over what a rebase changed in the branch's patch against `--main` — counted by `lib/utils/ReworkCountUtil.ts` from what `lib/platform/ReworkDiffs.ts` reads. Reads the current directory or `--worktree`, never the tracker, whose root is the main checkout rather than the branch under review. With both options the rebase is measured up to `<commit>`, so no line is counted twice. Read-only; no threshold is built in |
| `cli/rework/ReworkCommand.spec.ts` | a scratch repository taken through a builder commit, a review commit, a conflicting main-line commit and a hand-resolved rebase: the main line's work in neither count, the resolution in the rebase count, a clean rebase at 0, comments and documentation excluded, `--worktree`, both options in one call, and each refusal at exit 1 |
| `cli/release/ReleaseCommand.ts` | `release <id> [<id>...] --branch`: inside the one `openTrackerForWritingThenReadNextLine` hold — the lock is what serialises two releases — check each ticket is in-progress or in-review, the main checkout (the tracker root) is on `--main` and the branch descends from it, `git merge --ff-only` the checked commit, then move each ticket done and delivered through `applyTicketTransition`, and finish and deliver at the release time every `running` row whose `reviewOf` names a released ticket (`runningReviewRowsOf`; a row nested on the page by its name alone is not moved). Every refusal is thrown before the merge and leaves main and the tracker as they were, the review rows included; `main-moved` is the one a reviewer acts on. The worktree removal and `branch -d` run after the lock and are reported, never failed, at exit 0. The human output ends with the Next line, read after the delivery inside the lock hold; `--json` prints `{released: true, tickets, branch, mainLine, commit, closedReviewRows, cleanup}` on success and `{released: false, reason, detail, cleanup: []}` on every refusal, and carries no Next line |
| `cli/release/ReleaseCommand.spec.ts` | a scratch repository with the tracker in its main checkout and the work on linked worktrees: the fast-forward with the ticket delivered and worktree and branch gone, a bundle, the running review rows it delivers — one, a bundle's two beside an earlier delivered round and a name-only row it leaves, none on `main-moved`, and the hook's tokens still added to the delivered row — a run from inside the worktree, an untracked file left standing at exit 0, each refusal leaving main, both tracker files and the worktree unchanged, two concurrent releases never both merging, the `--json` key sets and the four cleanup step shapes read from `cli/HelpText.ts` and `skill/Reference.md` and held to what is printed, and the Next line ending the human output — agreeing with `status --json` afterwards — absent from `--json` |
| `cli/ticket/TicketCommand.ts` | `ticket add\|list\|show\|start\|claim\|review\|rereview\|done\|deliver\|abandon\|reopen\|status\|link\|depends\|priority\|agent\|hold\|unhold`: the markdown tickets and the rows they drive. `hold [--reason]` and `unhold` set and remove the frontmatter's `hold` key with one log line each, refused on a delivered or abandoned ticket and when nothing would change, and an unhold of an in-progress ticket whose row is paused ends its human output with a line naming a single-ticket dispatcher run that resumes the build, and, when the row's note is a dispatcher claim, saying the next whole-board run resumes it and the single-ticket run is the fast lane when none is going or about to be launched; a held ticket is refused by `claim`. `add --model/--effort` and `agent` name what a ticket's agents run on, `agent` refused on a delivered or abandoned ticket and when the resolved pair would not change. The named verbs enforce the legality matrix and `ticket status` is the override; `rereview` is the one verb legal on the status the ticket already holds; `show` always prints the file path. `claim <id> [<id>...]` is `start` plus the row's `--owner` and `--note` for every ticket named, as **one agent**: each row gets the same `agent` key (the claimed ids joined), and it **refuses** where `start` only warns: an unsettled dependency outside the claim (one on a ticket in the same claim counts as settled), a low ticket while a normal or high one is neither delivered nor abandoned, a ticket whose review bar is running, or agents in flight already at the concurrency limit — every check and every move inside the one `openTrackerForWriting` callback, so a bundle is all or nothing and two racing claims cannot both pass the count. `--json` prints the ticket for one id and the list for several. `review` and `rereview` with `--start-review` also add the round's running review bar (`Review <N> #<id> — <title>`, `reviewOf` set, `--owner` and `--note` naming it), `rereview` closing the previous round's bar first, in the same lock hold, so the builder's slot passes to its reviewer without ever showing free. Every `--json` ticket spells out its `priority`, `normal` where the file has none. The intake moves — `add`, `priority`, `agent`, `depends` and a move to `open` (`reopen`) — end their human output with the running dispatcher notice while the state read in the same lock hold is `running` |
| `cli/ticket/TicketCommand.priority.spec.ts` | priority: a low ticket filed with no row and no task id taken, its row created by `start` and kept on reopen, `abandon` and `clear` adding none, `ticket priority` removing and filing the row with one log line each and its refusals, the ready order and the low hold-back in `status --json`, `claim` and `start`, and a file without the key reading normal everywhere and left byte-identical |
| `cli/ticket/TicketCommand.agent.spec.ts` | model and effort: both stored by `add` and printed by `show` and `list`, `ticket agent` changing one with one log line, every refusal — bad values, no option, no change, delivered, abandoned — leaving the whole tracker byte-identical, and a file without the keys reading as opus/medium and left byte-identical by every reader |
| `cli/ticket/TicketCommand.hold.spec.ts` | `hold` and `unhold`: the `hold` key written only while held so a round trip leaves the file byte-identical, one log line each way, the unhold of an in-progress ticket with a paused row ending its human output with the whole-board run that resumes it and the single-ticket fast lane, the single-ticket run alone under a person's pause note, and no other unhold, hold or `--json` document carrying that line, `status --json` listing a held ticket in review in `heldTicketIds` and marking a held ready ticket's entry, `claim` refusing a held ticket, and every refusal — delivered, abandoned, already held, not held — leaving the whole tracker byte-identical |
| `cli/ticket/TicketCommand.dispatcher.spec.ts` | the running dispatcher notice: `ticket add\|priority\|agent\|depends\|reopen` end their human output with it, once, while the dispatcher is `running`, and print none while it is `finished` or `stopped`, nor under `--json`; `ticket claim`, the dispatcher's own move, prints none while it runs |
| `cli/ticket/TicketCommand.claim.spec.ts` | `claim`: the owner, note and single log line, each refusal leaving both files byte-identical, a running free-standing row counting toward the limit, and two concurrent claims for one slot of which exactly one exits 0; a bundle claimed beside another agent as one agent with one key on its rows, its `--json` list, refused whole with every file byte-identical on the limit, a dependency outside the claim or a started ticket, claimed whole when one member waits on another, holding its slot until its last ticket goes to review, and a ticket named twice claimed once |
| `cli/ticket/TicketCommand.review.spec.ts` | `--start-review`: the ticket in review beside a running bar that reviews it with the agents in flight unchanged, a plain `review` freeing the slot for a claim, a claim racing the move for the last slot refused under the lock, `rereview` closing the round's bar and numbering the next from the `## Review` sections, a claim refused while the bar runs, and `--owner` or `--note` refused without the flag |
| `cli/concurrency/ConcurrencyCommand.ts` | `concurrency [<n>]`: print the limit the board holds (2 on a tracker that never set one, `CONCURRENCY_LIMIT_CEILING_AGENTS` for a stored one above it), or store a new whole number from 1 to that ceiling under the lock with one log line. A limit below the agents in flight is accepted |
| `cli/concurrency/ConcurrencyCommand.spec.ts` | the default, a file with no limit field reading 2, a stored value read back, the refusals — 11 among them — leaving the file byte-identical, 10 accepted, a hand-built 12 reading 10 in the command and `status --json`, and a lowered limit giving `status --json` no free slot |
| `cli/dispatcher/DispatcherCommand.ts` | `dispatcher [running\|finished\|stopped] [--run <runId>]`: print the stored state without a lock (`stopped` on a tracker that never set one, writing nothing), or store one under the lock with one log line. `running --run` stores the Workflow run id a killed run is resumed by; every write without `--run` clears it. An unknown word, `--run` beside another state or none, and an empty id are refused at exit 1 before anything is written |
| `cli/dispatcher/DispatcherCommand.spec.ts` | a fresh tracker and a file without the field reading `stopped`, the latter left byte-identical, each state stored and read back, the refusals leaving the file byte-identical, a malformed stored state at exit 2, `status --json` carrying it, and the three advisories ending `status`'s Next line; the run id stored, printed and carried by both `status --json` documents, cleared by each write without `--run`, its refusals, and a malformed stored id at exit 2 |
| `cli/ticket/TicketCommand.spec.ts` | filing — two adds queued behind a held lock each headed with their own id, and the new ticket file written once, after the progress file, with its `dependsOn` — every transition stamping both files, the matrix and the same-status refusal, the review rounds `rereview` counts and what it refuses, the abandon refusal, `--tokens`, listing, showing, and linking |
| `cli/range/RangeCommand.ts` | `range`: the stored axis. A relative bound is stored verbatim; `kind` is `absolute` only when both ends are timestamps |
| `cli/range/RangeCommand.spec.ts` | each stored shape, including the mixed pair, and the refusals — an unreadable bound and a `--from` that is not before its `--to` |
| `cli/render/RenderCommand.ts` | `render`: regenerate the page under the lock, mutating nothing; an unreadable tracker is exit 2 through `renderDashboardOrRefuse`, reported once |
| `cli/render/RenderCommand.spec.ts` | an unreadable progress file at exit 2 with its reason on one line of standard error and no path printed |
| `cli/open/OpenCommand.ts` | `open`: render only when the page is missing, then hand the path to the desktop, detached — and print it either way; a missing page that cannot be rendered is exit 2, as for `render` |
| `cli/open/OpenCommand.spec.ts` | no page and an unreadable progress file: exit 2, reported once, no path printed and no page written. The happy path is not driven, since it launches a browser |
| `cli/clear/ClearCommand.ts` | `clear`: empty the rows and the log, keep `trackerId`, `project` and the task id counter, re-seed a row per surviving ticket from its frontmatter, except a low ticket that has no row. `--all` deletes the tickets |
| `cli/clear/ClearCommand.spec.ts` | the re-seeded bars and their fresh ids, the kept tracker id, `--all`, and both halves of the confirmation |

Adding a command is one entry in `cli/CommandTable.ts`, one block in `cli/HelpText.ts`, one folder
here, and one row above. `cli/CommandTable.spec.ts` and `cli/HelpText.spec.ts` fail until all four
exist.
